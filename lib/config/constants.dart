class Constants {
  static const Duration entryAnimationDuration = Duration(milliseconds: 1500);
  static const Duration delay1 = Duration(milliseconds: 1500);
  static const Duration delay2 = Duration(milliseconds: 3000);
}
